.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:topics, :index)
      %li.active=link_to pat(:new), url(:topics, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:topic)
    .inner
      -form_for :topic, url(:topics, :create), :class => :form do |f|
        =partial "topics/form", :locals => { :f => f }

-content_for :sidebar, partial("base/sidebar")
