.group
  =f.label :string
  =f.error_message_on :string
  =f.text_field :string, :class => :text_field
  %span.description Ex: a simple text


.group.navform.wat-cf
  =f.submit pat(:save), :class => :button
  =f.submit pat(:cancel), :onclick => "window.location='#{url(:states, :index)}';return false", :class => :button
